<% provide(:title, @book.name) %>

<div class="row">
  <aside class="col-md-4">
    <pre>
      <section class="user_info">
        <h1>Book name   : <%= @book.name %></h1>
        <h1>Book price  : <%= @book.price %></h1>
        <h1>Book number : <%= @book.number %></h1>
        <p type="button" ><%= link_to 'Add to cart', set_cookies_path(@book) %></p>  
      </section>
    </pre>
  </aside>
</div>
